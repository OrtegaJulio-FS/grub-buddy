VITE_API_URL=http://localhost:3000

# Mapbox public token (starts with "pk.") for the map view on the Feed page.
# Get one free at https://account.mapbox.com/access-tokens/ - public tokens
# are meant to ship in client-side code, unlike Cloudinary's API secret.
VITE_MAPBOX_TOKEN=
